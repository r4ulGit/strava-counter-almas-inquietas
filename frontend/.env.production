# Production Lambda Function URL — update after deploying the API Lambda
VITE_API_URL=https://4tmnmle654hfpiku73chw3p7ia0tbyjg.lambda-url.eu-west-1.on.aws/

# Auth keys — must match the values configured in the Lambda console
# Set these in the Vercel project settings (do NOT commit real secrets)
VITE_API_KEY=
VITE_API_SIGNING_SECRET=
